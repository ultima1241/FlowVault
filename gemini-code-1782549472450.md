# MASTER BUILD DIRECTIVE: ARCHITECTURAL DEEP-REFACTOR & VISUAL TRANSFORMATION

## 1. Core Mandate & Execution Philosophy
You are directed to act as an expert software engineer and UI/UX designer specializing in the Stellar/Soroban ecosystem. Your task is to ingest the provided baseline repository and systematically refactor, re-theme, and restructure it into a completely unique, independent standalone submission. 

The application must share **zero visual, naming, or structural fingerprints** with the original repository. However, it must remain 100% functionally complete, fully working, and operational. You must adaptively clone the underlying architectural robustness while building a completely indistinguishable frontend and distinct contract profiles.

---

## 2. Competitive Selection Framework (Mandatory Criteria for Project Acceptance)
Historical evaluation patterns demonstrate that project selection does not depend on idea complexity, but on rigorous compliance and **evidence discipline**. Projects with identical core smart contracts win or fail purely based on how transparently their compliance can be verified. You must strictly enforce the following execution standards to guarantee acceptance:

### A. Heading-for-Heading Checklist Alignment
* The final project README must match its headings **word-for-word** with the official challenge criteria. Do not group them into abstract summaries; layout the documentation so a reviewer can mechanically pattern-match checklist compliance instantly.

### B. Transparent Inter-Contract Documentation
* You must include a dedicated **"Inter-Contract Calls"** section in the README. 
* This section must explicitly name the exact technical mechanism used (e.g., `env.invoke_contract::<ReturnType>`), list the target function signatures, and state precisely which modular workflows trigger cross-contract communications.

### C. Cryptographic Evidence Validation
* Every contract address table and recorded transaction hash must have correct cryptographic string parameters before being committed to documentation.
* **Format Constraints:** Transaction hashes must be exactly 64 lowercase hex characters (`0-9a-f`); contract addresses must be exactly 56 characters starting with the character `C`. 
* Ensure every artifact points directly to its corresponding live endpoint on `https://stellar.expert/explorer/testnet/` using functional markdown hyperlinks. No broken, contradictory, or structurally invalid strings are permitted.

### D. Verification Screenshot Arrays
* You must compile a dedicated, highly organized screenshots repository in your documentation. You are required to capture and bundle:
  1. A fully mobile-responsive interface capture (~375px viewport configuration).
  2. A verified green, passing status run from your GitHub Actions CI/CD tab.
  3. A clean, verbose shell output printout from an executed `cargo test` run showcasing active assertions.

---

## 3. Global Codebase Obfuscation & Restructuring

### A. Naming Schema & Variable Overhaul
* **Functional Renaming:** Change all core smart contract methods, storage keys, events, and client-side hooks globally using completely distinct functional synonyms. 
  * *Streaming Vault Alternatives:* Map `create_stream` or `withdraw` to `allocate_asset_stream`, `initiate_vesting_lock`, `release_matured_capital`, or `claim_vested_yield`.
  * *Staking Pool Alternatives:* Map `register_stake` or `claim_rewards` to `initiate_yield_lock`, `provision_principal`, or `harvest_accumulated_incentives`.
* **TypeScript & State Identifiers:** Overhaul all interface definitions, data transfer objects, component names, and local React state structures to break any exact keyword pattern-matching with the reference repo.

### B. Directory Architecture & Path Re-routing
* **Folder Hierarchy Transformation:** Do not maintain the exact file mapping layout of the base repository. Alter the directory layout within the frontend workspace (e.g., re-routing `/src/components` and `/src/hooks` into specialized feature modules such as `/src/modules/vault-interface` or `/src/core/handlers`).
* **File Architecture:** Break up large, singular component sheets into modular atomic sub-files, or combine separate layout scraps cleanly to rewrite the physical layout footprint entirely.

---

## 4. Full Challenge Checklist Functional Requirements

You must ensure that the newly refactored implementation fully addresses and bakes in every single milestone of the official program criteria across all tiers. No placeholders, truncated code snippets, or `// TODO` items are allowed.

### Level 1 — Core Infrastructure & Basic Transactions
- **Wallet Authorization:** Seamless integration with Freighter wallet on the Stellar Testnet.
- **Connection Framework:** Explicitly designed `Connect` and `Disconnect` visual trigger elements.
- **Balance Interface:** Live client-side fetch and clean presentation of the active wallet's native XLM balance.
- **Transaction Pipeline:** Send native XLM transactions across the testnet, displaying real-time success/failure feedback states accompanied by active transaction tracking strings.

### Level 2 — Multi-Wallet & Smart Contract Binding
- **Multi-Wallet Adapter:** Implementation of `@creit.tech/stellar-wallets-kit` (StellarWalletsKit) to support multi-wallet selection panels.
- **Granular Exception Handling:** Distinct, dedicated visual error states handling exactly three fallback modes: Wallet Not Found, Signature Rejected by User, and Insufficient Network Balance.
- **Contract Execution:** Direct execution layer connecting to testnet-deployed contracts, facilitating both read-only operations and write contract invocations from the user interface.
- **Status & Sync Tracking:** Active tracking for state synchronization and live visual feedback mapping transaction transitions through Pending ➔ Success or Failure.

### Level 3 — Advanced Architecture & Production Readiness
- **Inter-Contract Cross Invocations:** Genuine execution of smart contracts communicating with secondary token contracts or native wrappers on-chain.
- **Real-Time Data Streaming:** Live client-side data streaming structures capturing contract lifecycle events to update the UI instantly.
- **Responsive Adaptations:** Fully optimized, mobile-first design system showing fluid adaptations down to ~375px (iPhone SE) and ~768px (tablet viewports).
- **Automated Verification Workflows:** A complete GitHub Actions workflow sheet configured to execute automated workspace linting, contract compilation, and deep test suite executions on every repository push.

---

## 5. Sub-Project Technical Specifications

Depending on the specific target target architecture you are implementing, the refactored code must fully map out the mathematical paradigms below:

### A. Asset Streaming Module (Target Spec A)
* **Vesting Calculus:** Linear calculations following the exact mathematical model:
  $$\text{vested\_amount} = \text{deposit} \times \frac{\min(\text{elapsed\_time}, \text{duration})}{\text{duration}}$$
* **Inter-Contract Routing:** Triggering capital releases through cross-contract invocations pointing directly to the target Token or native SAC contract instance wrapper via `env.invoke_contract`.
* **Live UI Ticker:** A high-precision client-side updater incrementing the accessible asset balance smoothly in real-time based on system clock offsets.

### B. Yield Aggregator & Staking Module (Target Spec B)
* **Incentive Accrual Model:** Compound linear checkpointing modeled precisely around basis point ratios:
  $$\text{accrued\_rewards} = \text{unclaimed\_balance} + \frac{\text{principal} \times \text{apy\_bps} \times \text{elapsed\_seconds}}{10000 \times \text{SECONDS\_PER\_YEAR}}$$
* **Checkpoint Synchronization:** Every invocation of deposit or withdrawal actions must first trigger a contract checkpoint evaluation to freeze accumulated incentives onto the previous balance tier before updating the primary capital pool weight.
* **Distribution Mechanics:** Claims for rewards must interface directly with a custom minting token contract to dispense proprietary distribution tokens cleanly to the signer.

### C. Automated Market Maker / Token Swap Pool (Target Spec C)
* **Invariant Model:** Enforce a strict constant-product liquidity profile across operations:
  $$x \times y = k$$
* **Slippage Protection Matrix:** UI configuration components tracking explicit slip limits, ensuring write transactions fail immediately if runtime parameters slip beyond boundaries:
  $$\text{amount\_out} < \text{min\_amount\_out}$$
* **Liquidity Invariant Tests:** Unit verification paths verifying that contract-level liquidity matrices ($k$) can never decrease through standard trading actions.

---

## 6. Technology Stack & Environment Parameters

Ensure the workspace environment compiles cleanly under the standard tech matrix:

| Layer | Configuration Standards |
|---|---|
| **Smart Contracts** | Rust + Soroban SDK (Latest stable compilation targets) |
| **Frontend Core** | Next.js 14+ (App Router architecture) with strict TypeScript |
| **Output Pipeline** | Static compilation exports (`output: 'export'`) |
| **Wallet Connector** | `@creit.tech/stellar-wallets-kit` (StellarWalletsKit) |
| **Styling Base** | Tailwind CSS configured with a totally custom visual design token sheet |
| **Deployment Anchor** | Cloudflare Workers Static Assets (`wrangler.toml` integration) |
| **Continuous Integration** | GitHub Actions workflows checking formatting and tests |

---

## 7. Radical UI/UX Design System Metamorphosis

### A. Complete Aesthetic Pivot
* **Anti-Template Directive:** Do not implement the standard "dark glassmorphism crypto-dashboard" styling standard across baseline web3 templates. Choose **one** of the following intentional design styles:
  * *Option 1 (High-End Fintech Minimalist):* High-density light mode styling, crisp off-white or charcoal surfaces, precise hairline borders, stark professional typography scales, and a premium, clean application layout.
  * *Option 2 (Modern Neo-Brutalist):* High-contrast flat layouts, stark solid geometric borders (2px–3px), heavy monochrome font weights, and vibrant primary-color accent anchors.
  * *Option 3 (Cyberpunk Technical Monochromatic):* Hyper-dense slate/dark layout scales, clean glowing monospaced typography matrices, clean technical graphs, and terminal-inspired contextual grids.
* **The Visual Centerpiece:** Build a customized, premium visual display for the core interactive element (the live vesting or APY accumulation numeric ticker), incorporating fluid layout tracking and bespoke transition dynamics.

---

## 8. Development Workflow & Commit Routine

You must avoid massive, single-batch workspace drops. Build out the repository incrementally, pushing code footprints through a sequential, 10-stage development branch routine containing a minimum of 12–15 distinct git commits:

1. `chore: project scaffold and architectural layout routing`
2. `feat: implement customized core contract module layers`
3. `test: compile functional contract testing suite matrix (5+ tests)`
4. `feat: integrate wallet authorization workflows via StellarWalletsKit`
5. `feat: implement tailored core feature interactive dashboard UI`
6. `feat: configure dedicated client state handling and live ticker polling`
7. `feat: build robust system error boundary components and loading fallbacks`
8. `feat: optimize mobile responsive layouts and breakpoint adaptations`
9. `ci: establish automated continuous integration workflows`
10. `docs: populate canonical README matching verification guidelines`

*Operational Boundary: If your environment lacks local terminal tools to directly broadcast transactions to the testnet or compile the Soroban packages, output the complete, bespoke source file trees, then provide the operator with the exact CLI execution strings to complete the on-chain pipeline manually.*